<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página inicial | Cattlebuy</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body class="fundo">
  <header class="container"
    style="width: 98%; max-width: 100%; border-radius: 0; border-top: 0; margin: 0; padding: 10px 20px; position: fixed; top: 0; z-index: 100; background-color: #001f3f;">
    <div style="display: flex; justify-content: space-between; align-items: center;">
      <h1 style="margin: 0; color: #FFF; font-size: 1.5rem;">Cattlebuy</h1>
      <nav class="menu">
        <ul style="display: flex; gap: 20px; list-style: none; align-items: center;">
          <li><a href="/perfil"><img src="/images/perfil.png" alt="Perfil"
                style="width: 25px; height: 25px; border-radius: 50%;"></a></li>
          <li><a href="/sobre" style="color: #FFF; text-decoration: none; font-weight: bold;">Sobre</a></li>
          <li><a href="/ajuda" style="color: #FFF; text-decoration: none; font-weight: bold;">Ajuda</a></li>
          <li><a href="/logout" style="color: #FF6B6B; text-decoration: none; font-weight: bold;">Sair</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main
    style="display: flex; flex-direction: column; margin-top: 70px; padding: 20px; width: 100%; max-width: 1200px; margin-left: auto; margin-right: auto;">
    <h2 style="color: #FFF; border-bottom: 1px solid #F0F0F0; padding-bottom: 10px;">Lotes Disponíveis</h2>

    <% if (lotes.length === 0) { %>
  <p style="color: #FFF;">Nenhum lote disponível no momento.</p>
<% } else { %>
  <% lotes.forEach(lote => { %>
    <div style="background-color: rgba(0, 51, 102, 0.7); padding: 15px; border-radius: 5px; margin-bottom: 10px; border: 1px solid #F0F0F0;">
      <h3 style="color: #FFF; margin-top: 0;">Lote #<%= lote._id %></h3>
      <p style="color: #FFF;"><strong>Raça:</strong> <%= lote.raca %></p>
      <p style="color: #FFF;"><strong>Sexo:</strong> <%= lote.sexo %></p>
      <p style="color: #FFF;"><strong>Peso médio:</strong> <%= lote.peso %> kg</p>
      <p style="color: #FFF;"><strong>Dentição:</strong> <%= lote.denticao %></p>
      <p style="color: #FFF;"><strong>Medicamentos:</strong> <%= lote.medicamentos.join(', ') %></p>
      <p style="color: #FFF;"><strong>Produtor:</strong> <%= lote.usuario ? lote.usuario.nome : 'Desconhecido' %></p>
      <p style="color: #FFF;"><strong>Contato:</strong> <%= lote.usuario ? lote.usuario.numero : '-' %></p>
      <p style="color: #FFF;"><strong>Localização:</strong> <%= lote.usuario ? lote.usuario.cidade + ', ' + lote.usuario.estado : '-' %></p>
    </div>
  <% }) %>
<% } %>
  </main>
</body>

</html>
